namespace MathApp
{
    public class Menu : Scene
    {
        protected override void OnActivate()
        {
            base.OnActivate();

            // if (ApplicationSettings.IsQuickPlay)
            // {
            //     // UIMultiplayerView multiplayerView = Context.UI.Open<UIMultiplayerView>();
            //     // multiplayerView.StartQuickPlay();
            // }
        }
    }
}
